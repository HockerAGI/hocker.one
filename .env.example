# ==============================
# HOCKER.ONE (Next.js)
# ==============================

# --- Supabase (PUBLIC: safe to expose to client) ---
NEXT_PUBLIC_SUPABASE_URL=https://YOURPROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Optional default project (client/server)
NEXT_PUBLIC_DEFAULT_PROJECT_ID=global

# --- Server-only: Nova AGI orchestrator ---
# IMPORTANT: NO "NEXT_PUBLIC_" aquí.
NOVA_AGI_URL=https://your-nova-agi-domain.com
NOVA_ORCHESTRATOR_KEY=__SAME_KEY_AS_NOVA_AGI__

# --- Commands signing (server-only) ---
COMMAND_HMAC_SECRET=__LONG_RANDOM_SECRET__

# Optional
# NODE_ENV=development

# ==============================
# NOTES
# ==============================
# 1) NOVA_ORCHESTRATOR_KEY debe existir SOLO en server env (no client).
# 2) NEXT_PUBLIC_* sí llega al browser; nunca pongas secretos ahí.