name: Completed Serverless Runtime Patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Patch already applied; this workflow is inert and will be removed before merge."
