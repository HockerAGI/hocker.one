---
document_id: HOCKER-CORE-HANDOFF-2026-08-19
status: ACTIVE
owner: Hocker One / Owner
classification: INTERNAL
captured_at: 2026-08-19T10:07:00-07:00
truth_order: production/configuration > main/migrations > executable contracts/tests > approved ADR/policies > canonical docs > vision/history
---

# HOCKER Core — handoff operativo y continuidad

Fuente activa de recuperación para Hocker One / NOVA / Core AGI. Todo hecho mutable debe reconsultarse antes de una mutación. El proceso previo a PR #230 permanece preservado en `HANDOFF_2026-08-19-PRE230.md`.

## 1. Cierre técnico de producción

PR #243 quedó **MERGED** el 2026-08-19.

- Candidate final: `e2cb93834e781e1f03132e767c646043413d8c36`.
- CI candidate: `32276318988` / #836 = `SUCCESS`.
- Job `Verify Hocker ONE`: regression tests, typecheck, lint, build y full dependency audit = `SUCCESS`.
- Preview exacto: `dpl_HkouMPbEWHdhfPeSNCH4pj7TYQHD` = `READY`.
- Merge `main`: `e48edf78ee2ed44d149543fa5680a3d6cb767c7a`.
- Vercel producción exacta: `dpl_81HqevySC8Ziq3KxQA1Gxy4Bw3ta` = `READY`, target `production`, metadata `githubCommitSha=e48edf78...`.
- Build producción errors-only: sin error; build completado.
- Runtime producción `error`/`fatal` en la ventana revisada: sin entradas.
- No se ejecutó deploy manual; la promoción fue por Git integration.

Este cierre técnico **no ejecutó** ni sustituye la ceremonia Owner AAL2.

## 2. Qué quedó en producción

- shell privado con seis destinos: Inicio, NOVA, Trabajo, Ecosistema, Operación, Más;
- dock móvil de cinco destinos: Inicio, NOVA, Trabajo, Ecosistema, Más;
- `/chat` NOVA inmersivo, sin dashboard chrome permanente;
- `/agis` compacto y decision-first, una acción Owner global y detalle técnico progresivo;
- acceso Owner/AuthBox simplificado sin cambiar `/api/auth/password-login`, Supabase Auth ni fallback `/owner`;
- provider/model/IDs fuera del primer plano normal;
- rutas secundarias conservadas vía contexto/búsqueda;
- documentación operativa, README y AGENTS alineados con la arquitectura vigente.

## 3. score-v3

`score-v3` es la versión vigente de certificación en producción.

- scorer determinista para misión, Owner Gate y ausencia de evidencia;
- corpus offline de las 16 AGIs canónicas;
- runner guarda `eval_scoring_version=score-v3`;
- idempotencia/reutilización exigen suite y scoring vigentes;
- snapshot no acepta feedback/evidence v1/v2 como v3;
- VERTX acepta lenguaje honesto equivalente de ausencia de evidencia sin aceptar claims positivos no verificados;
- `external_writes_executed=false` permanece como hecho determinista del runner de evaluación.

La certificación humana está **pendiente**, no fallida ni completada. No declarar 16/16 hasta obtener evidencia durable server-derived del flujo soportado.

## 4. Supabase reconsultado

Producción: `yvuibbcuntqpyqiuqggd`.

- estado: `ACTIVE_HEALTHY`;
- región: `us-west-1`;
- PostgreSQL: 17.6.1.063;
- 16 AGIs;
- 16 agentes;
- `allow_actions=true = 0`;
- 3 filas históricas `agi_eval_result`;
- 0 filas `agi_tool_eval_result`.

No se ejecutó DDL/RLS/grant destructivo para cerrar #243. Los WARN de Security Advisor continúan gobernados por `docs/security/SUPABASE_ADVISOR_EXCEPTION_REGISTER_2026-08-17.md`; Leaked Password Protection sigue `OPEN_PROVIDER_GATE`. Los `unused_index` de Performance Advisor permanecen INFO: no se elimina un índice sin workload/query-plan/dependency evidence y validación reversible.

## 5. Regla de rescate / limpieza aplicada

Todo elemento existente pasa por cuatro filtros: vigente y útil → conservar; útil pero viejo → reconstruir/adaptar; solapado → fusionar; sin valor para comprender/operar/recuperar/auditar → eliminar/descartar.

### PR #213

Cerrado sin merge como supersedido por #243.

- adaptado: login Owner/AuthBox limpio;
- fusionado por concepto: NOVA inmersiva, navegación simple, búsqueda y detalle progresivo;
- descartado: `NovaWorkspace` con tarjetas sobre chat, `WorkspaceControlsCard`, Signal workspace/state y chrome permanente;
- descartado: auth/MCP/backend antiguos no se mezclaron wholesale con el runtime vigente.

### PR #244

Cerrado sin merge. Se descartó su compactación destructiva del Ledger append-only y se conservaron sólo los hechos recuperables en fuentes activas.

### Workflows

Se conservaron los workflows que aportan cobertura real y ya están acotados por paths/concurrency/dispatch. No se eliminó cobertura útil sólo para reducir conteo.

## 6. Gmail vs. fuente de verdad

Gmail contenía avisos de fallos de SHAs anteriores (`5e27bf3`, `a33f9c5`) y un aviso de límite de Codex review. Esos correos son notificaciones históricas, no el estado actual. GitHub/Vercel reconsultados prevalecen: candidate final verde, PR #243 merged y producción exacta READY.

## 7. `nova.agi`

El último puntero durable previo para `nova.agi/main` fue `db417f262dfcddcad8e82f6be977415d0b0f3e89`; debe reconsultarse antes de tratarlo como estado actual.

El fallback dedicado sigue sin re-certificación runtime en este cierre. Antes de depender de él exigir: deployment/revision exacta → `/health/ready` → logs/heartbeat → E2E autenticado Hocker One→NOVA → routing/fallback → persistencia/telemetría → rollback.

## 8. Siguiente secuencia exacta

1. Owner humano entra a Hocker One producción y alcanza AAL2 real.
2. Desde `/agis`, ejecutar la certificación resumible `score-v3`.
3. No crear AAL2 sintético, no insertar eval rows manualmente y no usar service-role como sustituto de la ceremonia.
4. Reconsultar `agi_feedback`, runs/evidence y `allow_actions` después del flujo.
5. Cerrar Core certification sólo si las 16 AGIs tienen evidencia durable vigente y el snapshot server-derived las certifica.
6. Después, re-certificar `nova.agi` con evidencia runtime exacta.

## 9. Stop conditions

Detener certificación/promoción si catálogo !=16, aparece `allow_actions=true`, snapshot es parcial, AAL2 no es real, existen eval rows no atribuibles al flujo soportado, un probe intenta write externo, producción deja de apuntar al merge esperado o falta evidencia para afirmar un runtime externo.

## 10. Orden de recuperación

`AGENTS.md` → `docs/operations/INDEX.md` → este handoff → `LAST_KNOWN_STATE.md` → `PLATFORM_CLOSURE_2026-08-19.md` → `DOC_ALIGNMENT_2026-08-19.md` → reconsultar GitHub/Vercel/Supabase.
