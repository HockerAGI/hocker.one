---
document_id: HOCKER-CORE-HANDOFF-2026-08-19
status: ACTIVE
owner: Hocker One / Owner
classification: INTERNAL
captured_at: 2026-08-20T00:51:00-07:00
truth_order: production/configuration > main/migrations > executable contracts/tests > approved ADR/policies > canonical docs > vision/history
---

# HOCKER Core — handoff operativo y continuidad

Fuente activa de recuperación para Hocker One / NOVA / Core AGI. Todo hecho mutable debe reconsultarse antes de una mutación. El proceso previo a PR #230 permanece preservado en `HANDOFF_2026-08-19-PRE230.md`.

## 1. Release funcional y mantenimiento posterior

El release funcional promovido por PR #243 quedó verificado y permanece como baseline de producto:

- candidate final #243: `e2cb93834e781e1f03132e767c646043413d8c36`;
- CI candidate `32276318988` / #836 = `SUCCESS`;
- `Verify Hocker ONE`: regression tests, typecheck, lint, build y full dependency audit = `SUCCESS`;
- Preview exacto `dpl_HkouMPbEWHdhfPeSNCH4pj7TYQHD` = `READY`;
- merge funcional #243: `e48edf78ee2ed44d149543fa5680a3d6cb767c7a`;
- producción funcional inicial: `dpl_81HqevySC8Ziq3KxQA1Gxy4Bw3ta` = `READY`, exact `e48edf78...`.

Después se fusionaron #245, #246 y #247 para cerrar y volver autoestables los contratos de continuidad. PR #248 registró el cierre de mantenimiento Sonner/ESLint y `nova.agi` sin alterar runtime. PR #249 corrigió deuda transversal de tests de continuidad: dejó de fijar encabezados/copy y pasó a exigir evidencia semántica verificable (SHA, CI, Preview, producción, Supabase y Owner Gate).

Mantenimiento verificado posterior al release:

- PR #236, `@next/eslint-plugin-next 16.3.0 -> 16.3.1`: merge `09db7bff1382a9678130873739fb6fb263870cab`; cambio dev-only y gates Android/CI verificados antes de promover.
- PR #235, `sonner 2.0.7 -> 2.0.8`: exact-head `09e8f661e8c27c21d6d3ee5b1e9140d963f053eb`; CI #845, Android Signed Release #106, Android Debug APK #161 y Android Emulator QA #63 = `SUCCESS`; Preview `dpl_Gy4EV986wA3KSet8jLMMWPQRDXK2` = `READY`; merge `31de87b712a3327b6069426e48fbb5dc24e5d142`.
- PR #249: head `e1014f3d2227a378fb827de1ca127c292bf470c9`, merge `e6f12807bf705b11ef502ed53f4e4968c3620c7c`; preservó las garantías de continuidad sin depender de frases o encabezados efímeros.
- PR #250 reconstruyó de forma limpia el valor útil del Dependabot #233 sobre el `main` saneado. Candidate exacto `2426bc7454322d730ea656214b9c076dede17786`: CI #852, Android Debug APK #166, Android Signed Release #110 y Android Emulator QA #67 = `SUCCESS`; Emulator API 36 completó build, boot, instalación, launch, captura y no-crash; Preview `dpl_EujRiBuTuV6WC34u1DzYfG91niFQ` = `READY`. Merge #250: `f5e200973de637d243b2c83e8d079ff46a8eab80`.
- PR #251 cerró documentalmente el estado post-Next sin cambiar runtime.
- PR #252 exact-head `74c512b5b382e20f212e5db7c2c269e3261cd642`, merge `6905117dc5a6fd8c7925169755097f9d84ddf32f`: retiró las declaraciones Edge deprecadas de `/api/health/ping` y `/api/supply/orders` manteniendo Auth, roles, kill switch, idempotencia y permisos; CI #856 = `SUCCESS`, Preview `dpl_H1vCm8NLPNFhfCP1mkpFQ64csgUZ` = `READY`.
- PR #253 head `3714350df5263a9f9595874fcfaa554fa542e48f`, merge `269363d8b94db779277a1950c0649a33cee6b8c7`: reconciliación append-only del Ledger, documentación únicamente.
- PR #254 fusionó únicamente `LAST_KNOWN_STATE.md` y dejó `hocker.one/main=434c43524bff7020dba8b42acf18fcdb9516bfc5`; no cambió runtime ni infraestructura.
- **Producción observada en el corte actual:** Vercel `dpl_FhWhUWRbQ6brc7ZPB9m13QTYD9Hd` = `READY`, target `production`, metadata exacta `githubCommitSha=434c43524bff7020dba8b42acf18fcdb9516bfc5`; consulta `error`/`fatal` de las seis horas previas al corte sin entradas.

PR #233 quedó cerrado sin merge como supersedido. Gmail confirmó la razón operativa de su bloqueo: Dependabot detectó edición por otra cuenta y se negó a rebasarlo. No se forzó ese wrapper; #250 reconstruyó únicamente el delta útil desde `main` y lo sometió a la matriz completa.

**Regla autoestable:** un commit documental posterior puede mover `main`/Vercel sin alterar la autoridad funcional de #243 + mantenimiento verificado hasta #252. #253/#254 son continuidad/documentación; reconsultar GitHub/Vercel antes de usar un SHA o deployment como puntero live.

El cierre técnico no ejecutó ni sustituye la ceremonia Owner AAL2.

## 2. Qué quedó en el release funcional

- shell privado con seis destinos: Inicio, NOVA, Trabajo, Ecosistema, Operación, Más;
- dock móvil de cinco destinos: Inicio, NOVA, Trabajo, Ecosistema, Más;
- `/chat` NOVA inmersivo, sin dashboard chrome permanente;
- `/agis` compacto y decision-first, una acción Owner global y detalle técnico progresivo;
- acceso Owner/AuthBox simplificado sin cambiar `/api/auth/password-login`, Supabase Auth ni fallback `/owner`;
- provider/model/IDs fuera del primer plano normal;
- rutas secundarias conservadas vía contexto/búsqueda;
- documentación operativa, README y AGENTS alineados con la arquitectura vigente;
- Next `16.3.1` promovido posteriormente por #250 sin debilitar los contratos de hardening;
- rutas Edge deprecadas saneadas a Node por #252 sin ampliar permisos ni superficies de escritura.

## 3. score-v3

`score-v3` es la versión vigente de certificación desplegada en Hocker One.

- scorer determinista para misión, Owner Gate y ausencia de evidencia;
- corpus offline de las 16 AGIs canónicas;
- runner guarda `eval_scoring_version=score-v3`;
- idempotencia/reutilización exigen suite y scoring vigentes;
- snapshot no acepta feedback/evidence v1/v2 como v3;
- VERTX acepta lenguaje honesto equivalente de ausencia de evidencia sin aceptar claims positivos no verificados;
- `external_writes_executed=false` permanece como hecho determinista del runner de evaluación.

La certificación humana está **PENDIENTE**. No declarar 16/16 hasta obtener evidencia durable server-derived del flujo Owner soportado.

## 4. Supabase reconsultado

Producción: `yvuibbcuntqpyqiuqggd`.

- branch `main=FUNCTIONS_DEPLOYED`;
- preview project `ACTIVE_HEALTHY`;
- 16 AGIs;
- 16 agentes;
- `public.agi_agents.allow_actions=true = 0`;
- 51 `agi_runs` totales;
- evidencia desde 2026-08-19 concentrada en 3 AGIs distintas;
- último run durable observado: `2026-08-19 09:42:04.967363+00`;
- 3 filas históricas con `public.agi_feedback.feedback_type='agi_eval_result'`;
- 0 filas observadas `agi_tool_eval_result`.

La reconsulta de este corte confirmó el esquema vigente: `allow_actions` vive en `agi_agents` y el discriminador de feedback es `feedback_type`. No reutilizar consultas históricas que asuman esas columnas en otras tablas.

No se ejecutó DDL/RLS/grant destructivo para este cierre. Los WARN de Security Advisor continúan gobernados por `docs/security/SUPABASE_ADVISOR_EXCEPTION_REGISTER_2026-08-17.md`.

- Leaked Password Protection sigue `OPEN_PROVIDER_GATE`: no se simula un cierre mediante SQL.
- Los `unused_index` de Performance Advisor permanecen INFO: no se elimina un índice sin workload/query-plan/dependency evidence y validación reversible.
- Los warnings de GraphQL/`SECURITY DEFINER` se mantienen bajo el registro de excepciones y contratos RLS/RPC existentes; no se revocan permisos públicos intencionales sin verificar el producto consumidor.

## 5. Regla de rescate / limpieza aplicada

Todo elemento existente pasa por cuatro filtros: vigente y útil → conservar; útil pero viejo → reconstruir/adaptar; solapado → fusionar; sin valor para comprender/operar/recuperar/auditar → eliminar/descartar.

### PR #213

Cerrado sin merge como supersedido por #243.

- adaptado: login Owner/AuthBox limpio;
- fusionado por concepto: NOVA inmersiva, navegación simple, búsqueda y detalle progresivo;
- descartado: `NovaWorkspace` con tarjetas sobre chat, `WorkspaceControlsCard`, Signal workspace/state y chrome permanente;
- descartado: auth/MCP/backend antiguos no se mezclaron wholesale con el runtime vigente.

### PR #244

Cerrado sin merge. Se descartó su compactación destructiva del Ledger append-only y se conservaron sólo los hechos recuperables en fuentes activas.

### Dependencias

- #236 ESLint plugin patch: **conservar/promovido**, dev-only y gates completos.
- #235 Sonner patch: **conservar/promovido**, mejora de accesibilidad/estabilidad y gates completos.
- #250 Next `16.3.1`: **reconstruido/promovido**, candidato limpio sobre `main` con CI + Android + Emulator + Preview completos.
- #233 Dependabot Next: **descartado/supersedido**, cerrado sin merge; su valor útil fue reconstruido en #250.
- #234 Zod 4: **descartado para este ciclo**, cerrado sin merge.
- #237 Capacitor Android 8.5: **descartado para este ciclo**, cerrado sin merge.

### Workflows

Se conservaron los workflows que aportan cobertura real y ya están acotados por paths/concurrency/dispatch. No se eliminó cobertura útil sólo para reducir conteo. CI general conserva `paths-ignore: "**/*.md"`, por lo que un cierre exclusivamente Markdown no debe gastar una ejecución completa de aplicación.

## 6. Gmail vs. fuente de verdad

Gmail conserva alertas históricas. Para estado operativo, GitHub, Vercel y Supabase reconsultados prevalecen. No se archivaron, eliminaron ni modificaron mensajes durante este corte.

## 7. `nova.agi`

`nova.agi/main` fue reconsultado y el corte vigente observado es `5c022c5d95197e55fb4fc0eaab1a70d53224fcbf`, merge de PR #38 `docs(continuity): close dependency cleanup checkpoint`.

Ese cierre:

- codifica la regla HOCKER de rescate/limpieza en `nova.agi`;
- integra el checkpoint de dependencias #33/#36/#37 y deja #34/#35 fuera según su evaluación;
- alinea README/continuidad con `/health/ready`;
- no cambia provider/model/secrets/DDL/RLS/Owner Gate;
- **no prueba un deployment Railway vivo**.

No se observaron PRs abiertos en `nova.agi` en este corte. El fallback dedicado sigue sin re-certificación runtime demostrada. Antes de depender de él exigir: deployment/revision exacta → `/health/ready` → logs/heartbeat → E2E autenticado Hocker One→NOVA → routing/fallback → persistencia/telemetría → rollback.

## 8. Siguiente secuencia exacta

1. Owner humano entra a Hocker One producción y alcanza AAL2 real.
2. Desde `/agis`, ejecutar la certificación resumible `score-v3`.
3. No crear AAL2 sintético, no insertar eval rows manualmente y no usar service-role como sustituto de la ceremonia.
4. Reconsultar `agi_feedback`, runs/evidence y `agi_agents.allow_actions` después del flujo.
5. Cerrar Core certification sólo si las 16 AGIs tienen evidencia durable vigente y el snapshot server-derived las certifica.
6. Después, re-certificar `nova.agi` con evidencia runtime exacta.
7. Mantener Leaked Password Protection como gate del proveedor hasta que exista una acción Auth soportada y verificable; no simular cierre mediante SQL.

## 9. Stop conditions

Detener certificación/promoción si catálogo !=16, aparece `allow_actions=true`, snapshot es parcial, AAL2 no es real, existen eval rows no atribuibles al flujo soportado, un probe intenta write externo, producción deja de apuntar al release esperado o falta evidencia para afirmar un runtime externo.

## 10. Orden de recuperación

`AGENTS.md` → `docs/operations/INDEX.md` → este handoff → `LAST_KNOWN_STATE.md` → `PLATFORM_CLOSURE_2026-08-19.md` → `DOC_ALIGNMENT_2026-08-19.md` → `docs/00-governance/HOCKER_DEVELOPMENT_LEDGER.md` → reconsultar GitHub/Vercel/Supabase.
