---
document_id: HOCKER-CORE-HANDOFF-2026-08-19
status: ACTIVE
owner: Hocker One / Owner
classification: INTERNAL
captured_at: 2026-08-19T09:23:00-07:00
truth_order: production/configuration > main/migrations > executable contracts/tests > approved ADR/policies > canonical docs > vision/history
---

# HOCKER Core — handoff operativo y continuidad

Fuente activa de recuperación para Hocker One / NOVA / Core AGI. Todo hecho mutable debe reconsultarse antes de una mutación. El proceso previo a PR #230 permanece preservado en `HANDOFF_2026-08-19-PRE230.md`.

## 1. Producción vigente

- Hocker One `main`: `5ec9de77cbe38ec869b15b30f10ea455c11436f9`.
- Vercel producción: `dpl_3QFyHXnHmQUvry1WsyTCQov1fVtZ`, `READY`, exact `5ec9de77...`.
- Ningún cambio de PR #243 se considera producción en este corte.
- No se ejecutó deploy manual para promover el candidato.

## 2. Candidato funcional de PR #243

Diseño Owner aprobado: `docs/superpowers/specs/2026-08-19-hocker-one-ux-limpia-design.md`.

Último head **funcional** verificado antes de este checkpoint documental:

- SHA: `44bd2dbf4c389406819de88b6c309d4efe9cec2c`.
- CI: `32274977052` / #835 = `SUCCESS`.
- Job `Verify Hocker ONE`: regression tests, typecheck, lint, build y full dependency audit = `SUCCESS`.
- Preview exacto: `dpl_GZxLr7AWaeuVcapXrjDCc3h6Mh8Y` = `READY`.
- Build errors-only: sin error; build completado.
- Runtime `error`/`fatal` del Preview en la ventana revisada: sin entradas.

Este documento forma parte de un checkpoint documental posterior a ese SHA. **Antes de merge, reconsultar el head exacto resultante de este checkpoint y exigir de nuevo CI + Preview para ese head.**

## 3. UX implementada

- shell privado simplificado con seis destinos conceptuales;
- dock móvil de cinco destinos;
- `/chat` NOVA inmersivo, sin dashboard chrome permanente;
- `/agis` compacto, una acción global Owner y detalle técnico progresivo;
- provider/model/IDs fuera del primer plano;
- acceso Owner simplificado sin cambiar `/api/auth/password-login`, Supabase Auth ni fallback `/owner`;
- términos visibles cortos y preferentemente en español;
- rutas secundarias preservadas por navegación contextual/búsqueda.

## 4. score-v3

`score-v3` ya no es trabajo futuro: está implementado y cubierto por contratos ejecutables.

- scorer determinista para misión, Owner Gate y ausencia de evidencia;
- corpus offline para las 16 AGIs canónicas;
- runner guarda `eval_scoring_version=score-v3`;
- idempotencia y reutilización requieren suite/scoring vigente;
- snapshot de certificación no acepta feedback/evidence de `score-v1`/`score-v2` como `score-v3`;
- VERTX acepta paráfrasis honestas de ausencia de evidencia sin aceptar afirmaciones positivas no verificadas;
- `external_writes_executed=false` permanece como hecho determinista del runner de evaluación.

La ceremonia humana sigue **PAUSADA**. El merge técnico no equivale a 16/16 certificado.

## 5. Evidencia durable actual de Supabase

Producción: `yvuibbcuntqpyqiuqggd`.

Última evidencia operativa ya verificada en este proceso:

- 16 AGIs;
- 16 agentes;
- `allow_actions=true = 0`;
- 3 filas históricas `agi_eval_result`;
- 0 filas `agi_tool_eval_result`;
- NOVA y SYNTIA conservan PASS histórico; VERTX conserva FAIL histórico; no reescribir historia.

Security Advisor fue reconsultado. Los WARN de GraphQL discoverability y SECURITY DEFINER continúan clasificados por `docs/security/SUPABASE_ADVISOR_EXCEPTION_REGISTER_2026-08-17.md` bajo RLS, ownership, límites, ACL y `search_path` fijos. Leaked Password Protection sigue `OPEN_PROVIDER_GATE`; el conector disponible no expone una mutación autenticada de Auth para cerrarlo.

Performance Advisor reporta múltiples `unused_index` como INFO. No se eliminó ningún índice: INFO no demuestra que un índice sea prescindible. Cualquier cleanup físico requiere workload/query plans, dependencias/FK y validación reversible primero.

No se ejecutó DDL/RLS/grant destructivo en producción durante este cierre.

## 6. Limpieza / rescate aplicado

Regla: conservar lo vigente; reconstruir/adaptar lo útil viejo; fusionar solapamientos; descartar lo que no ayuda a comprender/operar/recuperar/auditar.

### PR #213

Cerrado **sin merge** como supersedido por #243.

- rescatado/adaptado: login Owner/AuthBox limpio;
- absorbido por concepto: NOVA inmersiva, navegación simple, búsqueda y detalle progresivo;
- descartado: `NovaWorkspace` con tarjetas sobre el chat, `WorkspaceControlsCard`, Signal workspace/state y capas decorativas permanentes;
- descartado: auth/MCP/backend antiguos no se mezclan wholesale con el runtime actual.

### PR #244

Cerrado sin merge. Su intento de compactar el Ledger append-only se descartó para no borrar historia válida. Los hechos verificables se rescatan en las fuentes activas sin reescribir el historial.

### Workflows

- CI general: conservar;
- Android Debug / Emulator / Release: conservar por cobertura específica y filtros de paths/concurrency;
- Lighthouse Diagnostics: conservar; sólo `workflow_dispatch`.

No se encontró duplicación que justifique eliminar cobertura.

## 7. `nova.agi`

`nova.agi/main`: `db417f262dfcddcad8e82f6be977415d0b0f3e89`.

Sigue como fallback dedicado no re-certificado. Antes de depender de él exigir: deployment/revision exacta → `/health/ready` → logs/heartbeat → E2E autenticado Hocker One→NOVA → routing/fallback → persistencia/telemetría → rollback.

## 8. Siguiente secuencia exacta

1. Revalidar el head documental de PR #243 con CI completo y Preview exacto.
2. Revisar threads/reviews y protección de `main`.
3. Promover PR #243 sólo con `expected_head_sha` exacto y gates verdes.
4. Verificar `main` y autodeploy productivo exacto; revisar build/runtime errors.
5. Mantener la ceremonia Owner separada del merge.
6. Cuando corresponda, Owner humano alcanza AAL2 y ejecuta certificación resumible `score-v3` desde UI.
7. Sólo después de evidencia durable 16/16 puede cerrarse Core certification.
8. Continuar con re-certificación dedicada de `nova.agi`.

## 9. Stop conditions

Detener promoción/certificación si el exact head no está verde, producción no apunta al merge esperado, catálogo !=16, aparece `allow_actions=true`, snapshot es parcial, AAL2 no es real, hay eval rows no atribuibles al flujo soportado, un probe intenta write externo o falta evidencia para afirmar un runtime externo.

## 10. Orden de recuperación

`AGENTS.md` → `docs/operations/INDEX.md` → este handoff → `LAST_KNOWN_STATE.md` → `PLATFORM_CLOSURE_2026-08-19.md` → planes/spec UX → reconsultar GitHub/Vercel/Supabase.
