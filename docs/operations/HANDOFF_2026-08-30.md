---
document_id: HOCKER-CORE-HANDOFF-2026-08-30
status: ACTIVE
owner: Hocker One / Owner
classification: INTERNAL
captured_at: 2026-08-31T04:48:39Z
truth_order: production/configuration > main/migrations > executable contracts/tests > approved ADR/policies > canonical docs > vision/history
---

# HOCKER Core — handoff operativo y continuidad — 2026-08-30

Fuente activa para Hocker One, NOVA y Core AGI. Antes de cualquier mutación se reconsulta GitHub, Vercel, Supabase y proveedores mutables.

## 1. Execution State actual

| Campo | Estado vigente |
| --- | --- |
| `baseline_sha` | `6ee2daa96843df9302e4fe874b7a06cf33147569` |
| `production_deployment` | Vercel `dpl_JBHvhDaCiQCtrztDQ7Zoci5AvYSV` — READY — production — alias `hockerone.vercel.app` |
| `database_migration_head` | `20260830153247` |
| `nova_runtime_revision` | `nova.agi/main` `5575e671c2931a5bf6304a968aae0490d67ca9c5` |
| `hocker_node_agent_revision` | `hocker-node-agent/main` `a4af22a97dc639389a4ba5f3dc8926ec95fa84ff` |
| `agi_certification_version` | `2026.08.21-8` + `score-v5` |
| `phase` | `EXPANSION_READY` + production-readiness hardening continuo |
| `next_gate` | NOVA Workspace / OperationalState + Owner/AAL2 + NOVA live-runtime + recovery evidence |
| `rollback_candidate` | Requery current READY production deployment immediately before any rollback; no stale fixed rollback ID is authoritative. |

## 2. Baseline certificado

- Core AGI certification: **16/16 AGIs, 48/48 cases PASS** under `2026.08.21-8` + `score-v5`.
- Tool evaluation: **19/19 read-only PASS**, `external_writes_executed=false` during certification.
- `allow_actions=true`: **0/16**.
- Core certification record: GitHub `#303` + Supabase durable evidence. Do not reopen unless certification contract/evidence changes materially.

## 3. Producción Hocker One

- Current protected `main`: `6ee2daa96843df9302e4fe874b7a06cf33147569`.
- Vercel production deployment for that SHA: `dpl_JBHvhDaCiQCtrztDQ7Zoci5AvYSV`, state **READY**.
- `/api/health/ping`: **HTTP 200 / online**.
- Runtime `error/fatal` review on that deployment: no entries in the verified window.
- Current security headers include `x-hocker-topology: 12.7L-2C-private-control-noindex` and `x-robots-tag: noindex, nofollow, noarchive`.
- `main` remains protected; required GitHub status is `Verify Hocker ONE`.

## 4. Nova / Hocker-node-agent

### nova.agi
- `main`: `5575e671c2931a5bf6304a968aae0490d67ca9c5`.
- Fastify patch `5.12.1` already integrated and verified by repository CI.
- Repository is green/protected.
- **Live Railway runtime remains unverified**. Issue `nova.agi #31` is the canonical external-runtime gate.
- Required evidence: exact Railway revision, `/health`, `/health/ready`, recent `nova-runtime-1` heartbeat, worker tick, one real read-only MCP call, one mutation deferred to Hocker One Owner Gate, no secrets in payloads/logs, rollback.

### hocker-node-agent
- `main`: `a4af22a97dc639389a4ba5f3dc8926ec95fa84ff`.
- Current patch-level maintenance is integrated.
- Protected `main`, required check `verify`.
- No current code change is required for expansion; local allowlisted/signed execution and no master cloud credentials remain the architectural boundary.

## 5. Supabase current state

- Project: `yvuibbcuntqpyqiuqggd`, PostgreSQL 17, `us-west-1`.
- Migration head: `20260830153247`.
- AGI canonical view: `canon_version=12.6C.1B`, `registry_profiles=16`, `runtime_agents=16`, `canonical_memories=16`, `specialized_feeds=16`, `enabled_tool_assignments=34`, `shadows_enabled_tools=0`.
- `agi_agents`: 16; `allow_actions=true`: 0.

### Security hardening already applied
- The four reviewed RPC paths have fixed `search_path=''`; own-history functions were changed to `SECURITY INVOKER`; explicit grants are used.
- `agis_public_catalog` was removed from unnecessary anon/authenticated GraphQL exposure.

### Advisor findings still open
Current Security Advisor still reports:
- anonymous public GraphQL exposure: `cashback_tiers`, `free_round_tiers`, `promo_offers`;
- authenticated GraphQL discoverability for operational/financial/audit/observability relations including `audit_chain`, `audit_exports`, `balances`, `bets`, `commands`, `deposit_intents`, `events`, `kyc_requests`, `llm_usage`, `nodes`, `profiles`, `project_members`, `observability_alerts`, `observability_incidents`, and others;
- public `SECURITY DEFINER` execution for `get_public_leaderboard` and `get_public_recent_wins`;
- `auth_leaked_password_protection` disabled.

These are **not automatically confirmed data leaks**. Existing RLS/consumer contracts must be reviewed before permission changes. No broad REVOKE is authorized merely to silence Advisor warnings.

## 6. Master-plan audit — implementation reality

### Completed
- Phase 0 baseline was established conceptually and the active operational index/handoff/closure structure exists.
- Phase 1 Core certification: complete at certification scope.
- Primary Supabase `SECURITY DEFINER` hardening: complete for the four reviewed functions.
- Phase 9 dependency/release work already executed in the current cycle: Supabase JS, Lucide, Gradle 9.7.1 and coordinated Capacitor 8.5.0 have been integrated and production web deployment is healthy.

### Partially complete / requires explicit evidence
- Agentic Security Gate: certification and action boundaries are strong, but the full dedicated injection/tool/memory/cross-tenant/approval/MCP security-eval pack is not currently proven as a fresh complete set in the active state source.
- NOVA Runtime + Capability Fabric: code exists, but dedicated Railway runtime evidence is still missing.
- Owner AAL2 / Context Bridge: AAL2 enrollment/enforcement exists; real human negative-path/containment and full Context Bridge AAL2 activation evidence remain open.
- Observability/SLO/FinOps: current logging/health exists, but the master plan's complete SLO/alerts/provider-cost evidence is not yet closed.
- Backup/restore/continuity: restore drill with measured RPO/RTO is not yet proven in the active source.
- Android final API 36 gate: requires a fresh manual run on the final `main`; PR evidence alone is insufficient.

### Not complete on current `main`
- The requested `NovaWorkspace` component decomposition is **not yet implemented by that name**.
- `NovaRealtimeChat` remains a large monolithic client component; `NovaRealtimeChatLazy` still wraps it.
- The full frontend `OperationalState` contract from the master plan is not present as that named abstraction; the DB view `v_agi_operational_state` is narrower and is not equivalent.
- Full NOVA Workspace capability picker/history/real files/voice/artifacts experience has not been proven as complete on `main`.
- Full responsive/device certification matrix (320px through TV), browser accessibility certification, and visual regression evidence are not yet complete.
- Final release-candidate evidence pack / continuity publication is not yet complete.

## 7. Source-branch rescue policy

There are numerous stale `nova/*`, UX and MCP branches. They are **not production truth**. Existing work may be salvaged only after exact diff review, current-main rebase, dependency impact review and the normal PR gates. Do not merge a historical branch wholesale.

A particularly relevant historical branch is `nova/nova-chat-ux-foundation-127kux1`: its chat component contains capability-picker, action-draft and integration UX ideas absent from current `main`, but its source predates the current baseline and must be selectively ported, not merged wholesale.

## 8. Open blockers / external gates

- `hocker.one #166` — real Owner AAL1/AAL2 negative-path + containment.
- `hocker.one #167` — Context Bridge activation bound to Owner AAL2; legacy route retirement only after proof.
- `hocker.one #183` — Supabase Advisor/GraphQL authorization reconciliation.
- `hocker.one #200` — Leaked Password Protection; manual Supabase Auth setting.
- `hocker.one #203` — Android API 36 final-main manual QA.
- `hocker.one #210` — Cloudflare provider-side MCP/Worker boundary evidence.
- `hocker.one #211` — PUNTO·G governance/catalog decision; remains outside current Core implementation.
- `hocker.one #212` — isolated Supabase branch unavailable on current Hobby plan; do not pretend a branch exists.
- `hocker.one #296` — ESLint 10 compatibility evaluation.
- `hocker.one #290` — setup-java 6.0.0 independent maintenance candidate.
- `hocker.one #301` — PDFKit 0.20.1 independent compatibility candidate.
- `nova.agi #31` — Railway live runtime verification.

User-confirmed credential rotation is complete; do not reopen #181 as a blocker unless new evidence contradicts that state.

## 9. Current operating rule

Hocker One is **EXPANSION_READY**: new integrations/projects may be built on this baseline. However, `production_ready` remains a separate closure state until the external/human and master-plan evidence gates above are genuinely satisfied.

New work must follow: investigate → design → branch/PR → tests → exact-head Preview → merge → production → smoke/health/logs → continuity update.

No fake integrations, no synthetic AAL2, no broad grants, no dummy CI commits, no direct production DDL without the defined validation path, and no claim that a historical branch is current runtime truth.
