# HOCKER ONE — ACTIVE HANDOFF / 2026-09-13

## Authority

- Repository: HockerAGI/hocker.one
- Current main: `0994e6df5cba44c2929605a21f01eac595a721d5`
- Latest merged milestone: Operating Loop v1 contract slice (PR #368).
- Prior unified stream milestone: PR #365 -> `2ab17d56468ba47ca980f138b3679bcc99367f30`.
- Production must be re-queried before every mutable action; historical deployment IDs are not authority.

## Current architecture

- NOVA remains the primary orchestrator/control plane.
- 16 AGIs remain specialist identities; users should not manually select the specialist for ordinary missions.
- Capability/Tool routing uses the canonical Hocker Fabric.
- Native MCP tools are model-facing contracts; Hocker One remains the execution/policy/evidence boundary.
- Dynamic MCP providers enter through governed manifests and HTTPS host allowlisting.
- IA↔IA delegation uses canonical `agi_tasks`/`agi_runs`, parent lineage and bounded depth/fan-out.
- SYNTIA learning reuses the existing Learning Extractor/Memory Mirror path.
- `/api/nova/chat/stream` uses the unified Hocker runtime before the dedicated `nova.agi` compatibility fallback.
- Operating Loop is an orchestration envelope over existing state, task, action, evidence and Owner Gate infrastructure; it does not create a second authority.

## Current merged milestones

1. History workspace — PR #335.
2. MCP Tools surface — PR #336.
3. Agentic Security Gate — PR #337.
4. Repository Workspace — PR #338.
5. Runtime-truth correction — PR #340.
6. Automatic AGI routing — PR #346.
7. Dynamic MCP provider fabric — PR #351.
8. Bounded IA↔IA + SYNTIA learning — PR #360.
9. Unified NOVA streaming — PR #365.
10. Continuity after unified streaming — PR #366.
11. Operating Loop contracts — PR #368.

## Current AGI evidence

Direct production evidence must be re-queried before claims about current liveness.

Known durable baseline:
- `2026.08.21-8` + `score-v5`
- 16/16 AGIs
- 48/48 PASS
- 19/19 read-only tool evaluations PASS
- `allow_actions=true`: 0/16

This durable baseline does not establish fresh runtime liveness.

## Operating Loop v1

Design source exists in PR #367:
`docs/superpowers/specs/2026-09-10-hocker-operating-loop-v1-design.md`

Implemented first slice in PR #368:
- `WorkSessionState`
- transition validation
- `ResearchRecord`
- `ExecutionCandidate`
- `ApprovalEnvelope`
- `WorkSessionEnvelope`
- deterministic canonical SHA-256 hashing

No new memory store, queue, MCP registry, runtime router or production action path was introduced.

Next planned slices, in order, must be implemented only after verifying current main for existing equivalents:
1. Work Session persistence wrapper over existing records.
2. Executable Research Gate.
3. Deterministic execution candidate builder.
4. Scoped Owner Approval Envelope over existing Owner Gate/AAL2.
5. Resumable orchestration runner.
6. UX consolidation behind NOVA.

## Remaining hard gates

- Fresh 16/16 runtime certification.
- Dedicated `nova.agi` physical runtime certification.
- Node Agent physical heartbeat.
- Owner AAL1/AAL2 ceremony and negative-path evidence.
- Backup/restore and measured RPO/RTO.
- Full files/artifacts/voice/browser/code-execution capability parity.
- Supabase provider-plan leaked-password protection gate.

## Release rule

Never merge a stale branch merely because it once had green checks. Required sequence remains:

`current main → exact-head CI → exact-head Preview → review → protected merge → production exact-SHA → health/smoke → evidence → documentation reconciliation`.

Manual gates remain consolidated at the end.
