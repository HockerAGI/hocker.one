# HOCKER ONE — Active Handoff 2026-09-05

Status: **ACTIVE**
Evidence cut: **2026-09-05 08:39 UTC**
Scope: **Hocker One + NOVA + canonical AGI Core**

## Authority

Production/configuration + DB/logs/reproducible evidence > main/migrations/executable tests > approved ADR/policies > current canon > historical narrative.

A Preview proves only its exact SHA. Registry presence does not prove runtime liveness.

## Current verified pointers

- Hocker One main: `83bbc890ce8a48c0eaf559998d09ddcfe3be6138`.
- History feature merge: `b1b8554fd7f9c7091634df6487687472a1d0e127` (PR #335).
- History production deployment: `dpl_GX27gsB9ZBe5fvYjPDvsGuFTBDy3`, READY, exact SHA `83bbc890...`.
- NOVA main: `2e3d2f8c8c577b88678267fd02b96c37afa1f567`.
- hocker-node-agent main: `3fd8b8adaf6ad4bae66c432536bd955552f0f0f6`.
- Supabase production: `yvuibbcuntqpyqiuqggd`.
- Supabase migration head: `20260903182025_align_queue_orphan_view_with_reconciler`.

## Hocker One — completed

- NOVA History is now production-promoted.
- Canonical persistence remains `agi_sessions + agi_messages`; no second history store.
- `/api/nova/history` is owner/project scoped.
- `/chat?thread_id=...` restores the persisted conversation.
- Preview exact-head passed before merge.
- Production build passed compile, TypeScript, static generation and final optimization.
- Public smoke: `/chat` returned HTTP 200.
- Private history/runtime endpoints without identity returned HTTP 401.
- No production runtime errors observed in the post-merge window.
- Capability contract, capability registry, capability router and `NovaWorkspaceCapabilities` picker already exist and remain the canonical path.
- Owner Gate, queue lock, action queue, Context Bridge, MCP registry and provider routing remain intact.

## Current operational freshness

- `agi_agents = 16`.
- `allow_actions=true = 0`.
- `agi_runs` last 24h = 0; latest recorded run `2026-08-30T02:48:30Z`.
- `agi_tasks` last 24h = 0.
- `agi_integration_checks` last 24h = 0; latest `2026-08-10T06:17:05Z`.
- `nodes` seen last 24h = 0; latest recorded node signal `2026-08-18T09:52:48Z`.
- Result: **registry is verified; fresh AGI execution and physical node liveness are not verified.**

## NOVA runtime

- nova.agi main contains the provider-neutral hosting decision and OCI deployment kit.
- Railway is not operational authority.
- Runtime remains **fallback candidate / unverified** until exact revision produces:
  health, readiness, heartbeat, worker tick, read-only MCP, Hocker One authenticated E2E, fail-closed mutation handoff, secret leak check and rollback evidence.

## Node Agent

- Current main: `3fd8b8a...`.
- Security contract remains HMAC + project/node scope + allowlist + cloud rejection + sandbox + non-root + allow_write + telemetry.
- Physical heartbeat is not currently evidenced.

## History incidents — closed

- #330: discarded.
- #331: discarded after inconsistent branch/Preview history; its production path is not used.
- #334: discarded; Vercel failure was isolated to TS7006 in `NovaRealtimeChat.tsx`.
- #335: clean rebuild from current main, merged and promoted.

## Next software gate

**GATE — Operational Evidence Recovery**

1. Identify the exact missing producer for AGI runs/integration checks.
2. Verify whether current Hocker One runtime can legitimately trigger read-only/evaluation activity without a dedicated runtime.
3. Do not manufacture heartbeat or runs.
4. Prepare all software-side runtime evidence before the single final manual ceremony.
5. Only after operational evidence is sufficient, continue:
   History (done) -> Capabilities (partial/implemented) -> Tools -> Connectors -> AGIs -> Actions -> Approval -> Evidence.

## Manual batch remains final

Keep together:
AAL1/AAL2 negative-path + containment, OCI provisioning for nova.agi if retained, physical Node Agent heartbeat, Android API 36 current-main QA if required, Cloudflare provider-side gate only if applicable, Supabase leaked-password setting, and real restore/RPO/RTO drill.

## Do not repeat

Do not rebuild memory, OperationalState, capability registry, MCP registry, Owner Gate, action queue, Context Bridge or provider routing. Do not reintroduce Supabase Realtime. Do not treat repository registration or old certification as live liveness.

## Release rule

Every material change:
research -> design -> risk -> TDD -> minimal change -> CI -> exact-head Preview -> review -> protected merge -> production -> health/smoke -> documentation.
